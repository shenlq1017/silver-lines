# 银幕金句 · M4「1000 经典」备料与门禁

> 拍板日：2026-09-25 · 统筹：New Bot · 验收：小记·项目经理
> 当前锚点：`origin/main @ fb34638`；本地 `quotes.json` 为 C1 并入后的 `published=274` 目标，线上以 origin 为准（Pages Top249）；rank 108 茶馆已永久跳过。

## 1. 目标

- 长期目标：**约 1000 部经典电影 + 语录**（片库与素材先行，上架分批）。
- 当前 Top250 已上架至 Top249；后续以质量为先，结合 classics-ext material 与新金句继续策展。

## 2. 硬约束（全阶段）

| 项 | 要求 |
|----|------|
| 编码 | 沙箱本地；**禁止 Cursor/云端编码代理；禁止云端操作** |
| 推送 | **禁止擅自 push**；素材包 / 架构改动过 PM 验收后由 PM 推，或 PM 明示放行 |
| 上架 | 按用户/PM 明确批次执行；本地 `quotes.json` 为 C1 并入后的 `published=274` 目标，线上是否已推以 origin 为准；不得无验收扩量 |
| 素材 | TMDB **original**；禁 w500、禁外链热图；本地 `assets/` |
| 静帧 | 宽边优先 **≥2560**；源站最大不足 → 入库最大档并标 `meets_2560:false` + note；**<1920 不上** |
| 海报 | 同既有 Top250 门禁（高清 JPEG，过瘦/异常单独标记） |
| 精选 | `featured` 维持约 5 条，除非产品明确要求改变 |

## 3. 已上线与批次状态

- Top250 主批次 **M6–M12 全部 live**；M7/M8 历史跳过的条目已通过后续 gap-fill 处理。
- rank 141/142 已上线：`heat-sun-always-summer`、`nezha-one-deed-alone`。
- classics-ext 海报批次 `batch-jia`、`batch-jia2`、`batch-yi`、`batch-bing`、`batch-yi2` 均已 live。
- rank 108 茶馆已依据 `/workspace/tmp/teahouse-108-still/report.md` 永久跳过：无合法且宽边 ≥1920 的景观静帧，最大仍为 `1280×720`，低于 `<1920` 门禁。

## 4. 架构评审状态（当前）

- 评审结论：**条件 YES（data-driven）**；模板 + `scripts/sync-quote-pages.mjs` 路线成立。
- P0/P1 尚未实现：`scripts/check-ingest.mjs` 及配套 runbook 仍待补齐。
- 现有上架仍需素材、数据、详情壳与 PM 终验；架构评审的条件项不能视为已完成。

## 5. 上架节奏（与备料解耦）

```
备料过验 →（可选）金句 draft 包 → PM 策展验 → 并入 published + 详情同步
         → 沙箱冒烟 → PM 终验 → 推 main
```

- 默认下一批上架粒度：**用户确认后的一批**（建议 +25 或 +50，反对一次铺开数百条）。
- 无句 / 无合规静帧 / 无本地海报 → **不上架**。

## 6. 剩余工作

1. rank 108 茶馆：已永久跳过；如未来门禁或可用来源政策变化，再依据报告复核。
2. 可选清理或提交 `assets/posters/classics/` 下仍未跟踪的遗留海报。
3. 可选 P0：实现 `scripts/check-ingest.mjs` 与 runbook。
4. Top250 之外继续以 classics-ext material 与新金句策展至约 1000。
5. Featured 维持约 5 条，除非产品明确要求改变。

## 7. 风险

1. 1000 目标易范围膨胀：片库元数据 ≠ 上架金句；PM 只认「备料包」与「上架包」两套门禁。
2. 低分辨率静帧会破坏详情体验；`<1920` 继续不上。
3. 遗留未跟踪素材需逐项核对 batch JSON，避免误提交或误删。

## 8. PM 回执口径

- 备料包：齐套 + 软例外清单 → **有条件通过 / 退回**；通过后由 PM 推或明示开发推。
- 模板 / ingest 门禁：架构评审条件 YES；`check-ingest` 与 runbook 完成并通过回归后，才可称 P0/P1 已落地。
- 上架包：策展 + 素材 + 冒烟 → 终验通过后 **PM 推 main**。
